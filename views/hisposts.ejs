<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Posts - ChessDeals</title>
    <style>
        body{
            font-family: Arial, sans-serif;
            background: linear-gradient(to right, #e16d50, hsl(26, 100%, 73%));
        }
        .card {
            background: linear-gradient(to right, #fc967d, hsl(26, 93%, 77%));
            width: 300px;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        .name {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 10px;
        }
        .rating {
            font-size: 18px;
            margin-bottom: 10px;
            color: #181818;
        }
        .email {
            font-size: 16px;
            margin-bottom: 10px;
            color: #3d3d3d;
        }
        .mobile {
            font-size: 16px;
            color: #4a4a4a;
            margin-top: 20px;
            border-top: 1px solid #5f5f5f;
            padding-top: 10px;
            margin-bottom: 19px;
        }
        h1{
            font-size: 30px;
            color: white;
        }
        .cardcont{
            display: flex;
            flex-wrap: wrap;
            gap: 25px;
            margin-top: 1vw 2vh;
            margin-bottom:2vh;
        }
        .price{
            color: #3d3d3d;
            margin-bottom: 10px;
        }
        #a{
            color: #006eff;
            display: inline-block;
            text-decoration: none;
            margin-top: 3px;
        }
        body{
            padding: 1vw 2vh;
        }
        
    </style>
</head>
<body>

        <h1>My Posts</h1>
        <div class="cardcont">
        <% if (posts.length === 0) { %>
            <p><%= message %></p>
        <% } else { %>
            <% posts.forEach(post => { %>
                <div class="card"> 
                    <div class="name"><%= post.name %></div> 
                    <div class="rating">Rating: <%= post.rating %></div> 
                    <div class="email">Email: <%= post.email %></div>
                    <div class="price">Price: ₹ <%= post.price %></div>
                    <div class="mobile">Mobile: <%= post.mobile %></div> 
                    <form action="/deletePost" method="POST"> <input type="hidden" name="postId" value="<%= post._id %>"> <button type="submit" class="delete-button">Delete post</button> </form>
                </div>
                
            <% }) %>
        <% } %>
        </div>
        <a href="/home" class="back-button">Go Back to Home</a>
    </div>
</body>
</html>
